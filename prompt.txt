gérère un prompt structuré dans un fichier .md, détaillé et sans ambiguité, permettant de générer une application angular, avec vitejs, qui permet d'effectuer des tests de masse d'une api rest. Le formulaire d'encodage des apramètres du test devra permettre d'encoder le verbe http (get, post, put, delete), l'url, les paramètres à passer en body, en header, via l'url,... (prévoie les possibilité courantes des apples rest, en te basant sur les possibilités offerte dans postman), on devra aussi pouvoir passer un fichier en param (donc en multi-part file), on devra spécifier les caractérisqtiques d el'exécution du test de masse : combien de virtulas user en //, pendant combien de temps lancer les appels (base toi sur les possibilité postman), on devra aussi avoir des résultats de test, comme une graphique avec une courbe pour les tests en erreur, les requetes envoyées par minutes, le temps de réponse moyen, maximum, minimum des résponses (prévoi de pouvoir cocher les courbes à afficher ou masquer sur le graph du rapport). il est imposrant aussi me mémoriser la taille de chaque réponse, car on va envoyer x fois la même requete, qui devrait retourner une réponse d emême taille (afin de pouvoir détecter des incohérences)